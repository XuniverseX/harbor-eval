# 仓库协作约定

- 新增或修改代码时使用中文注释和中文文档字符串。说明模块、函数的职责，以及关键配置、边界条件和非显然的实现原因，避免逐行复述代码。
- 每完成一个有文件改动的任务，先执行与改动相适应的检查，再创建 Git commit；提交说明应清楚描述本次任务。
- 提交只包含当前任务相关文件，不混入用户已有的无关改动。
- 不提交私有环境信息，包括密钥、服务地址、实际部署模型标识、个人路径、代理配置、机器信息及实际评测记录。只在本地 `.env.local`、`.local/` 或被忽略的运行目录中保存；公开文档和测试使用通用示例。
- 不提交下载的题库、运行日志或缓存；遵守 `.gitignore`。提交前检查暂存内容，避免通过注释、说明文档或提交说明泄露私有环境。
- 注释或文档改动无需重新运行付费模型评测；涉及运行逻辑时执行适配器测试，必要时再做端到端验证。
